import React from "react";

export default function NoToken() {
	return (
		<div>
			Would you like to play as a <button>Guest</button>
			<button>Login </button>or
			<button>Register </button>
		</div>
	);
}
